# PLAN

开始时间：2026-09-08 19:01

## M0 · 骨架 + 验证回路（预算 15 分钟） ✅ 实际 3 分钟（19:01–19:04）
index.html：importmap three.js，全屏 quad + ShaderMaterial，resize，uniforms iTime/iResolution/uAudio/uBeat/uScale。
fragment shader 先输出渐变，预留 map()/raymarch()/calcNormal()/render()。renderScale 用小 render target 拉伸。
window.__stats 和 ?test=1。verify.mjs。
验收：verify 对渐变通过。commit `M0: skeleton + verify loop`。

## M1 · 城市 SDF + 光照 + 雾（预算 25 分钟） ✅ 实际 4 分钟（19:04–19:08）
方案：
- 坐标系：相机沿 +z 走街道，街道在 x 方向周期 CELL=6，z 方向周期 CELL=6；街道宽 2.2（半宽 1.1），楼体占格子内剩余。
- map(p)：id=floor(p.xz/CELL)，局部 q=p.xz-(id+0.5)*CELL；hash22(id) 给楼的半宽 (0.9–1.6) 和高度；高度 = 2 + 26*pow(h,4)（幂律，少数高楼）。
- 相机所在的街道列 (id.x==0) 不放楼，保证有路可走；楼体 sdBox(q, halfsize, height)，顶部再加一个 0.4 倍的小 box 当天台机房。
- 因为楼不超出格子（半宽 ≤ CELL/2 - 1.1），格内 SDF 对相邻格是保守的（下界），domain repetition 安全；不做邻格采样。
- 地面 y=0 平面 SDF 一起 min。
- raymarch：128 步，t 从 0 到 200，阈值 0.001*t + 0.0005，远处退出。
- 法线：四面体四采样，eps 随距离放大。
- 光：keyLight 冷蓝 (0.55,0.65,0.85) 方向 normalize(0.4,0.7,-0.3)，半兰伯特 (0.5+0.5*NdotL)^2；天空环境 hemisphere 0.5+0.5*n.y。
- AO：5 步 SDF 采样，衰减系数 0.6，不超过 0.9 的暗度。
- 雾：exp(-0.045*t)，雾色 (0.16,0.15,0.16) 比天空略暖；远楼消失。
- 天空：底部暖灰 (0.28,0.22,0.19) 到顶部近黑 (0.02,0.025,0.04)，用 pow 曲线；无星。
- 材质：楼体 albedo 低饱和蓝灰 (0.28,0.30,0.34)，地面 (0.16,0.16,0.17)；暖色点缀留给 M3 的窗户，M1 只在地平线暖色。
- 曝光：col *= 1.6 后 gamma；亮度目标非黑 >40% 由天空+雾保证。

验收：截图是像样的夜城。commit `M1: city sdf + lighting + fog`。→ CP1

## M2 · 音频管线（预算 15 分钟） ✅ 实际 6 分钟（19:09–19:15）
点击开始遮罩 → AudioContext。麦克风按钮；拖 mp3/wav 播放。Analyser fftSize 2048，bass/mid/high/energy，滑动最大值归一，不对称平滑（0.4/0.08）。beat：bass > 1s 滑动平均 1.4 倍且 >200ms，uBeat=1 后 8Hz 指数衰减。右下角 5 根柱子，H 隐藏。?test=1 仍合成。
commit `M2: audio pipeline`。→ CP2

## M3 · 映射 + 相机 + 体积光（预算 25 分钟，硬止损） ✅ 7 步全部完成，实际 3 分钟（19:27–19:29）
方案：
- 相机 z 在 JS 里积分（速度依赖 energy，shader 里不能积分），新 uniform uCamZ；test 模式 uCamZ = 16。高度 2.6（平均楼高 7.6 的 1/3）+ 微 bob，横向 sin 漂移 ±0.6，俯仰 sin 微量。
- 新 uniform uParams = (响应强度, 雾倍率, 相机速度, 预留)，M4 滑杆直接用，默认 (1,1,1,0)。
- 楼高响应：cellParams 里 ht *= 1 + 0.6*bass*resp*uParams.x，resp = 0.2+0.8*hash。楼在 SDF 里真的变高，天台跟着走。
- 窗户在 shade() 里做：按法线选面坐标 (|n.x|>0.5 用 p.z 否则 p.x)，格 0.45×0.6，每格 hash < 密度则点亮，暖色 (1.0,0.72,0.44)，只在竖直面。密度 0.15+0.5*high。
- 雾密度 × (0.7+0.6*mid) × uParams.y。
- beat：ro 沿屏幕径向偏移 0.12*uBeat*uv（镜头"推"一下），曝光 × (1+0.12*uBeat)。
- 体积光：8 步沿视线到 min(t,50)，每步一次 map()，可见度 = clamp(map/2)（楼附近散射被挡），HG 相位朝主光，冷色。
- 反射：地面命中 → 反射 rd，march ≤32 步，shade(cheap=true)（无 AO、无体积光），fresnel 0.04+0.96*(1-cos)^5 × 0.8 湿度混入。
- shade(p,n,rd,t,mat,cheap) 抽出来给主射线和反射共用。

1. 相机沿街推进 速度 = 基础 × (0.6+0.8×energy)，横向漂移+俯仰，高度楼高 1/3
2. 楼高 × (1+0.6×bass)，每楼 hash 响应系数 0.2–1.0
3. 窗户 hash 网格，密度 0.15+0.5×high，暖色 #FFB870
4. 雾密度 = 基础 × (0.7+0.6×mid)
5. beat：射线原点径向扭曲 + 轻微曝光
6. 体积光 8 步；fps<50 减 4 步或砍
7. 地面 SDF + 湿地面反射 ≤32 步 fresnel
commit `M3: audio mapping + camera + volumetric light`。→ CP3

## M4 · 性能 + 面板（预算 15 分钟） ✅ 实际 3 分钟（19:33–19:36）
自适应 renderScale（<55 两秒 -0.1 下限 0.4；>59 五秒 +0.05 上限 1.0）。面板 ≤60 行：输入源、响应强度/相机速度/雾 三滑杆、beat 阈值、renderScale 显示。F 全屏 S 存 PNG H 隐藏，全屏 3 秒隐藏光标。
commit `M4: perf + panel`。→ CP4

## M5 · 对抗式审美审阅 + 录屏（预算 15 分钟） ✅ 实际 4 分钟（19:48–19:52）
subagent 审阅（配色/光影/动态各 ≤3 条），修影响最大 3 条。R 键 MediaRecorder 30 秒 webm 8Mbps。
commit `M5: polish after review`。→ CP5

## M6 · Look-dev（开始 20:14） ✅ 8 步全部完成，实际约 60 分钟（20:14–00:15，含一次中断）
方向：雨夜胶片。阴影深青蓝 #0B1622，光钨丝暖黄 #FFB36B，中间全黑。签名动作：beat 光圈从相机扩散点亮窗户。
方案：
- 后期：场景 pass 输出线性 HDR 到 HalfFloat RT（alpha 存深度 t/MAXD）；post shader 一个文本两个模式：mode0 亮部提取到半分辨率 RT（开 mipmap 当 4 级降采样），mode1 合成：ACES → bloom（4 级 mip 各 9-tap tent）→ anamorphic（lod 2.5 水平 17 tap，冷蓝）→ 色散 1.5px → 颗粒 → 暗角 → 2.39 黑边。
- 调色：天空/雾/楼面全部青蓝族，去掉街面暖色上射光，唯一暖色是窗户 + 泄光。
- 窗户：每楼 hash 选窗型；亮度四档按簇 hash；3x3 邻窗泄光染墙；竖直分布中层最密。
- 轮廓：h>0.95 退台 3 级 + 天线 + 红灯 1Hz；20% 楼顶水箱簇；mod(id.z,7)==3 整行空（河）；地标塔固定在相机前方 140 单位偏右 12，高 90。
- 空气：探照灯从塔顶，JS 积分角度（bass 调速），12 步体积采样只算它；雨在 post 屏幕空间；地面反射法线加涟漪；post 里按深度 2px 远景模糊。
- 相机：高 1.5，朝塔略偏，12 秒一次 3° 抬头 easeInOut，横滚 ±0.5°。
- 音频：uBeatAge（最近两次 beat 的年龄）→ 光圈 + 0.1s 闪电；bass → bloom 阈值 + 探照灯转速；high → 雨；energy → 速度。删 bass 楼高 / beat FOV / mid 雾。
- test 模式 URL 参数：t、beat（年龄）、search（角度）用于三张 CP6 截图；rec=N 录 N 秒。
- ffprobe 不在 PATH，用 playwright 自带 ~/Library/Caches/ms-playwright/ffmpeg-1011/ffmpeg-mac 数帧。

M6 决策：
- 后期分两个 pass 但仍是一个 post shader（uMode 0/1）；bloom 用半分辨率 RT 的 mip 链当 4 级降采样，每级 9-tap tent。
- 暗角放在线性空间 tonemap 之前：否则和 bright1 / dark5 两条指标互相打架（暗角压高光又抬不了黑）。
- black crush 0.012（display 空间）：ACES 脚趾 + gamma 会把线性 0.004 抬到 sRGB 0.045，光靠压环境光到不了 dark5<0.03。
- 窗户：任务书的 30/50/15/5 分布保留，但"暗黄"档取 0.10（0.35 经 ACES 仍像亮窗），加每楼 0.4–0.9 入住率，否则整墙白格子。
- 地标塔跟随相机（永远在前方 200），MAXD 320；探照灯下倾 35.5°，光柱能落进街道；体积采样集中在视线离光轴最近处 ±26 单位内 10 步 + 每步 1 次朝光源遮挡采样。
- 性能：map() 里屋顶结构和退台只在 p.y 高于相应高度时才算；塔先算包围盒；march 100 步、反射 20 步、AO 4 步 → fps 58–60。
- 光圈：环带内亮窗 ×2.5（任务书），另外点亮 45% 的暗窗到 1.2 并给墙面 0.015 暖洗，否则截图里看不出来；第一版点亮全部候选窗变成白格子，收回。
- 录屏帧数校验不用 ffmpeg（playwright 自带的是精简版），用 Chrome 回放读 totalVideoFrames；录制前关掉第一个窗口并热身 1.5 s，132 帧 → 149 帧。
- test 模式参数：`&beat=<age>`（光圈年龄秒）、`&search=<rad>`（探照灯方位）、`&rec=<s>`；verify 环境变量 EXTRA / OUT / NOREC。

## M7 · 从作品到成片（开始 00:24） ✅ 8 步全部完成（00:24–01:05）
顺序：1 立面材质 → 2 路灯/路缘/水洼 → 4 相机叙事 → 3 云 → 5 闪电稀有化 → 6 光圈纹理 → 7 FXAA → 8 车灯。每步 verify + commit。
方案：
- 立面：在 shade() 里做楼层带（按窗高 ws.y 分层，层缝 albedo ×0.55）、竖梃（窗列之间细线）、女儿墙（顶部 0.4 单位亮一档）、每楼 albedo 0.75–1.25 变化 + 微色相偏移；全部随 lod 退化。
- 路灯：每条南北街两侧、每街区一盏，高 3.2，钨丝色；SDF 里加灯杆 + 灯头（mat 6 自发光）；shade() 里查最近两盏做点光（向下锥）；沿相机所在街道的 8 盏用解析式点光雾散射（无 map 调用）。路缘石：格内 sdBox 抬 0.12。水洼：2 阶 value noise mask，水洼内镜面 fresnel 0.85，其他地方反射权重 0.35。
- 相机：按 z 对河周期（56）做关键帧：峡谷段低 1.5 → 河段升到 4.0 并转向塔 → 回落；uCamZ 驱动，test 可复现。新增 &camz= 参数。
- 云：skyColor 内把 rd 投到 y=60 平面，3 阶 value noise，云底暖灰被城市照亮，探照灯 beamAt(云点) 加冷色光斑；雾色用无云的 skyBase。
- 闪电：JS 每 8 个 beat 或 energy>0.85 触发，uFlashAge；test 用 &flash=。
- 光圈：每扇窗随机延迟 0–150 ms、衰减 4–8/s。
- FXAA：post 合成里对 tScene 做 mattdesl 版 FXAA（luma 取 tonemap 后），再做 DOF 和色散。verify 新指标：camz 16 与 16.02 两帧亮度差 >0.2 的像素比例。
- 车灯：shade() 地面上按街道坐标 + 时间画车头灯点光（只暖色），最近 2 辆。
- verify 新增：暖色像素占比上限（r > 1.3b 且 luma>0.2 的像素 ≤ 12%）。

M7 决策：
- 立面/色调必须在光照之前乘进 albedo（第一版乘在 col 之后只影响了泄光项）。
- 路灯：灯头只向侧下方发光（俯视顶面 0.15），否则俯瞰时远岸灯头连成白线；灯光 1.0、雾散射 0.0008，再高就把街抬灰。
- 河改两行宽（16 单位），相机抬升峰值在河前 10 单位；抬升段和河面段比峡谷段贵 15–25%，靠自适应 renderScale 兜底，verify 只卡峡谷段 camz=16。
- 第 6 步 verify 没过却 commit 了（管道吞退出码）→ RETRO 8 + CLAUDE.md 规则。
- flicker 指标意外抓到根本问题：fract(sin) 哈希跨编译不确定，M0 起每次加载城市都不一样。改整数哈希后 4% → 0.02%（位移 0.0005）/ 0.25%（位移 0.02）。阈值 0.6%。FXAA 对该指标几乎无影响（它去台阶不去爬行），保留作回归护栏。
- 性能：march 72 步 eps 0.002t、反射 10 步、AO 3、体积光 6 步、窗户邻域十字 5 点、路灯 SDF 只在 |lz|<1.5、FXAA 只算一次、色散只在边缘混入。canonical 帧 fps 50–53。
- 亮窗 3.0→1.8 仍被 ACES 推成白 → tonemap 改 60% 保色相混合。bloom 十字 5 点会在点光源周围出"十"字，回 9 点但从 mip 1 起。

- 基准帧改为峡谷段 camz=44（相机 1.5 高、近楼填满画面）：直方图和暖色指标是按峡谷帧设计的，河面远景帧（camz=20）用 EXTRA 报告不卡阈值。flicker 阈值 1.2%（AA 0.92 / 无 AA 1.06）。
- 亮窗最终 1.6、近白 4.5；tonemap 65% 保色相、l>4.5 才允许剪白。

## M8 · 作者性（开始 01:10）5 hold ✅ 其余 8 条完成（01:10–01:45）
顺序：2 光的层级 → 1 构图/镜头/前景 → 3 河面 → 9 片头片尾 → 4 天气过程 → 7 时域混合+运动模糊+24fps → 8 乐句级音频 → 6 调色。
方案：
- 2：入住率 0.4–0.9 → 0.25–0.5；探照灯下倾 35°→25°、落点到相机附近，衰减放缓，JS 算相机处的 beam 强度 uMood.z 给后期让前景雨幕被照亮；路灯/车灯各减 30–40%。
- 1：相机 x=-0.9、yaw +6°、pitch 4.5°（地平线在下三分线）；焦距 mix(1.7, 2.4, rise)；后期加 3–5 个镜头上的雨滴（小圆 + 折射偏移，每 4 s 换位置）。桥/高架按要求 hold，河面段前景暂用近处路灯。
- 3：河面 mat 2 且在河行：法线沿 z 强扰动（反射竖向拉长）、fresnel 0.95、albedo 0.004、不受路灯照。
- 9：开场 2 s 从黑淡入 + 角落标题 1–5 s；录制开始时重放淡入，最后 1.5 s 淡出；uMood.w = fade。test 模式 fade=1。
- 4：雨强度包络 sin²(t/17)、风改雨丝角度 sin(t/9)、雾密度乘 vnoise 雾团、涟漪幅度随雨量；scene 端用 uMood.x = rain。
- 7：历史 RT ping-pong，合成时与上一帧混合 25%（运动模糊 + 时域平滑，诚实命名，不是完整 TAA）；test 模式混合 0；面板加 24 fps 录制勾选。
- 8：4 s 能量趋势检测 drop（近 1 s 均值 > 前 3 s 均值 ×1.25 且有 beat）→ 唯一闪电 + 相机加速 3 s；安静段入住率 ×(0.6+0.4×慢能量)。uMood.y = quiet。
- 6：按街区（6×6 格）hash 给钠灯橙偏移；后期 lift/gamma/gain 分通道，阴影微青绿。
- verify 新增：亮斑连通区数量上限、亮度重心离中心的最小距离（先报告，做完 2 和 1 后定阈值）。

- 01:20 M8 第 2 步：入住率减半后 ≥0.9 亮度像素只剩 0.55%，bright1 按最亮 1% 算必然失败。改为最亮 0.5% 均值 >0.9（BRIGHT_FRAC=0.005）。甲方 Opera 里的页面把 GPU 分走一半，上一 commit 也只剩 30 fps；开发期间用 FPS_SOFT=1 只报告 fps，最后 GPU 空出来统一重跑。

M8 决策：
- 探照灯改 25° 下倾 + 落点到相机附近后，第一版光锥在河面帧成了半屏白雾，收窄到 1.2+0.035s、衰减 0.00012、体积强度 0.03 并封顶。
- 镜头雨滴：4 个小圆每 4 s 换位、缓慢下滑、内部取翻转的场景采样；test 模式静止。
- 时域混合 25% 用 ping-pong 历史 RT 在显示空间做，诚实命名为"运动模糊 + 边缘更稳"，不是完整 TAA；test 模式默认关，&taa=1 开，录制时开。
- drop 检测：近 1 s 能量均值 > 前 3 s 均值 ×1.25 且在 beat 上、间隔 >6 s；闪电只在 drop 或 25 s 无闪电时；drop 后 3 s 相机加速最多 1.8×；安静段入住率 ×(0.6+0.4×慢能量)。
- 调色：街区（6×6 格）hash 给 0–50% 钠灯橙偏移；后期 lift(0,0.006,0.009)/gain(1,0.99,0.97)/gamma(1,0.98,0.96)。
- verify 新阈值：重心偏离 ≥3%（基准帧 5.6%）、亮斑 ≤90（67）。
- 三次补丁因缩进不匹配未写入（assert 拦住，无静默错误）；两次 verify 因 GPU 被占的录屏帧数不足未 commit。

## M9 · 换引擎级更新（开始 01:38）5 hold
顺序：1 DDA 高度场追踪 → 2 TAA 重投影 → 5 节拍/段落 → 3 四机位剪辑 → 4 室内视差 + 街面语言 → 霓虹街区 + 雨雾 → 胶片颗粒 → 6 城市辉光 + 探照灯阴影。
决策：
- DDA：map() 拆成 lotSDF/slabSDF/lampSDF/towerSDF 四个精确组件；traceDDA 沿 xz 网格行走，每格先 AABB 求交再局部 sphere-trace ≤24 步；反射也走 DDA（副作用：湿地反射不再被 10 步截断，画面明显更完整）。A/B 与参考路径像素差 0.81%（阈值 2%）。uPath=0 保留参考路径。
- TAA：相机改 JS 单一来源（cameraAt），场景和后期共用；Halton 16 抖动；按上一帧相机重投影 + 深度一致性（5% + 0.4）+ 3×3 邻域钳制；历史权重 0.75；切镜头时清两帧。显示层混合降到 0.08。test 模式默认关（可复现），&taa=1 开。
- 节拍：谱通量 → 8 s 自相关（50 Hz）→ 抛物线插峰；置信 >0.35 时 beat 相位锁到网格（±80 ms）。段落：verse/build/drop/breakdown，最短 4 s。
- 机位：A 高空俯冲、B 街面、C 近岸看河、D 对塔长焦推；段落映射 verse→B、build→A、drop→D、breakdown→C；在 beat 上切，最短 6–8 s 最长 12 s；无音乐时 8 s 轮换。test 用 &rig=N，基准帧仍是 B。
- 室内视差：own-cell 亮窗做 interior mapping（后墙/天花灯/地板/侧墙 + 25% 有百叶）；街面加雨棚环（60% 中低楼）和店面（40% 面）。店面第一版 0.35 太亮成白板，收到 0.12。
- 霓虹：街区（6×6 格）hash<0.18 为霓虹区，楼有 35% 概率一条竖/横霓虹（其余区 3%），只用品红 (1,0.15,0.55) 和青 (0.15,0.85,1) 两色；霓虹作为点光染街和墙、解析散射做雨雾；两色规则的例外仅限霓虹区。camz=60 段是霓虹区。
- 颗粒：三阶 + 35% 色度 + 1.2 倍暗部 + 片门微动 0.7 px + 1% 闪烁。
- 光照：城市辉光半球光 0.02；探照灯朝光源 4 步 map() 遮挡（DDA 后付得起）。
- 电脑在电池 13% 放电，整机锁 30 Hz；GPU 计时也不可信；fps 一项全程 FPS_SOFT，插电后统一重跑。
- 三次补丁因缩进/保留字（half）被 assert 或编译器拦住，无静默错误。

## M10 · 氛围渲染（开始 11:20）✅ 12 条全部实现并 commit（11:20–11:50）；未做：CP10 截图、30 秒重录、插电后 fps/DDA 提速实测、RETRO 收尾
顺序：3+5 文字进 canvas + 歌词工具 → 1 水珠重做 → 2 世界空间雨 → 4 赛博朋克加浓 → 6 体积雾 → 7 蒸汽 → 8 景深 → 9 反射质量 → 10 调色 → 11 飞行器灯 → 12 分辨率。
方案：
- 文字：2D canvas 画标题/歌词 → CanvasTexture → 后期合成（颗粒暗角压在字上）；系统衬线栈 Bodoni 72/Didot/Georgia，细字重大字距，签名式常驻低透明度。DOM 标题隐藏。
- 歌词：tools/lyrics.swift 用 macOS 离线 SFSpeechRecognizer 输出 .lrc；页面接受 .lrc 拖入（与 mp3 同时或先后），按 player.currentTime 逐行淡入淡出；test 用 &lyric= 显示一行。
- 水珠：只在 A/C 机位；SDF 弯月面法线、折射 mip 模糊场景、背后有亮光才显形、拖痕、2–3 颗。
- 雨：三层世界空间雨幕（按深度视差），亮度 = 附近光（路灯/霓虹/探照灯）× 雨丝，天空前暗；近层运动模糊；地面溅花。
- 赛博朋克：霓虹区密度 0.6，四色板，全息广告牌（程序字形滚动，hi-hat 闪，beat glitch），街区主色雾团，灯光饱和度提升。
- 体积雾：DDA 预算 → 16 步异质雾，路灯/霓虹/探照灯在雾里成束，楼缝 god rays。
- 蒸汽：井盖/屋顶通风口噪声球体积蒸汽，路灯侧光。
- 景深：bokeh，切镜头拉焦 0.6 s。反射：按距离/粗糙度模糊，水洼干湿边。调色：分离调色 + 每机位曲线。飞行器：远景灯串。分辨率：插电后 1.0 / TAA 1.25。

- 11:40 M10：基准帧改到 camz=100（非霓虹带；44 正对着霓虹带 48–96）。亮斑上限 90→110：甲方明确要求更多霓虹和更鲜艳，光源数量本来就该上去；霓虹帧另用 BLOBS_MAX=200 报告。

- 11:50 M10 收尾：歌词工具走 LaunchServices 后授权成功，合成人声测试转写正确率高（lights→likes 一处）；甲方要求停止（credit 用尽）。

- 16:35 M10 收尾：解析 tracer（无 march）10.8 vs 参考 14.1 ms（0.75）、17.7 ms（1.0，电池）；歌词工具分段识别可用于清晰人声，音乐伴奏下离线识别为空——真歌请用手写/网上 .lrc；30 秒录屏（song.wav + song.lrc）机位 1→3→0→3，自适应 0.55–0.65（电池）。

- 已知问题：静帧（TAA 关）里剪影边缘有 1px 亮点——远处亮窗从屋顶边缘露出的走样，CA 修复后由绿变中性；动态 + TAA 下不可见。彻底修需要 test 模式也开 TAA 并等收敛后截图。

## M11 · 播放器（开始 22:10）✅
- 可行性四项先验后做：歌词服务可达且返回标准 LRC、中文歌覆盖 20/20、按时长匹配版本、浏览器直连（http 和 file:// 都通）、IndexedDB 存音频 blob 跨刷新可播（配额 2 GB）。
- 版权边界：不下载歌曲本身，音频由甲方提供且不进仓库；歌词做成页面的运行时查询功能（与任何音乐播放器同类），只本地缓存。
- 同名歌陷阱：《記得》第一版匹配到林俊傑的同名歌。修法是中英文件名拆段生成查询变体 + 候选必须歌手名对得上，再按时长距离排序。两首现在都是 Δ0s 精确命中。
- 歌单来源两条：http 下读 music/manifest.json（音频直接由服务器流式播放，不占 IndexedDB）；拖入的文件存 IndexedDB 跨会话保留。
- 中文歌词不用斜体（合成倾斜难看），改 Songti SC 衬线正体；文字带轻微阴影保证在亮处可读。

## 决策记录
- 2026-09-08 19:08 M1：第一版曝光像白天（albedo 0.24、key 0.85、雾 0.032 都按白天量级给的）。改为夜景量级：albedo 0.13、key 0.6×、雾 0.011、天空地平线 0.11。规则：夜景场景所有线性量从 0.1 量级起步。
- three.js 锁 0.170.0（jsdelivr）。RawShaderMaterial + GLSL3，自己声明 precision/out。
- 街区：CELL=8，街道半宽 2.0，楼半宽 1–2，楼在格内随机平移但不进街道；domain repetition 用「到格边界距离 + 2.0」做步长上界保证不穿邻格。
- 2026-09-08 19:15 M2：频带用 1/bin 加权平均近似对数频带；自适应增益用半衰期 2.5s 的滑动最大值；未接入音频时用 0.25 倍合成信号做"呼吸"。真实路径用 playwright + 合成 wav（60Hz kick 每 0.5s）验过：beat 触发、四值在动。
- 本地服务用 python3 -m http.server 5173（零安装，等价 npx serve）。
- 2026-09-08 19:29 M3：gMat 全局材质要在 calcNormal() 之前读（calcNormal 调 4 次 map 会覆盖它）。窗户第一版 2.4 倍亮度像乐高，改 0.9 倍 + 窗口缩小到格子的 40% + 远处按距离退化成平均亮度防闪烁。
- 2026-09-08 19:36 M4：自适应 renderScale 在 test 模式关闭（保证截图可复现）。面板 HTML 14 行 + JS 约 25 行。S 存帧依赖 preserveDrawingBuffer=true（M0 就开了）。
- 2026-09-08 19:52 M5 审阅 9 条，采纳 3 条：①体积光冷蓝 vs 雾暖棕互相抵消 → 体积光改同族暖中性色；②整图中灰无黑、屋顶比立面亮 → 去掉 ×1.25 曝光、albedo 0.09/0.03、月光 ×0.3、加街面暖色上射光；③beat 原点平移 + 10% 闪像 glitch → 改成 FOV 呼吸 + 3% 原点位移 + 3.5% 曝光。
- 不采纳并说明：地平线改冷色（任务书要求地平线偏暖、雾比天空略暖）；bass 楼高 0.6→0.1（任务书指定 0.6）；窗户 smoothstep 渐亮、湿地面 fresnel 0.12、窗户光晕 —— 有价值但超出"只修 3 条"，留作后续。
