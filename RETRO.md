# RETRO

## 1. M1 第一版曝光像白天（19:05，推翻自己方案）
- 症状：第一次 verify 通过但截图是白天雾城，楼面接近纯白。
- 根因：albedo 0.24、key 0.85、雾 0.032、天空地平线 0.30 都是按"看得清"的日景量级填的，没先定夜景的绝对亮度基准。
- 修法：所有线性量整体降一档：albedo 0.13、key ×0.6、雾 0.011、地平线 0.11，两轮才收敛。
- 下次如何避免：写第一版光照前先定 3 个锚点值（天空最亮处、楼面受光面、楼面背光面的线性亮度目标），其余参数往锚点推，而不是先写再调。

## 2. M3 窗户第一版像乐高（19:28，推翻自己方案）
- 症状：窗户占格子 50%、2.4 倍亮度、密度 0.4，整面墙是黄白方块，远处闪。
- 根因：窗户发光强度和尺寸没有参照"唯一暖色点缀"的定位，按"看得见"给的；远处没有 LOD。
- 修法：窗口缩到格子 40%，亮度 0.9，密度上限 0.5，远处按距离退化成平均亮度。
- 下次如何避免：点缀元素先给"占画面亮度多少比例"的目标（这里是 <10% 像素、峰值不超过天空最亮处 3 倍），再定参数。

## 3. gMat 被 calcNormal 覆盖（19:29，M3 第 7 步发现）
- 症状：写反射时意识到主射线的材质 id 是在 calcNormal 之后读的，交界处会串材质。
- 根因：map() 用全局变量传材质，任何额外的 map() 调用（法线、AO、体积光）都会改写它。
- 修法：raymarch 返回后立刻读 gMat，再算法线。
- 下次如何避免：规则——全局副作用变量必须在产生它的调用之后一行内消费；或者让 raymarch 直接返回材质。

## 4. 时间记录不可靠（19:29）
- 症状：PLAN.md 里 M2/M3 的时间戳是我估的，与 `date` 输出不符，改了两次。
- 根因：没有在每个里程碑开始/结束时真的跑 `date`。
- 修法：里程碑开始和 commit 前各跑一次 `date` 写进 PLAN.md。
- 下次如何避免：CLAUDE.md 加规则（已加）。

## 5. M6 第 5 步 shader 编译失败：函数定义顺序（20:41）
- 症状：verify 报 `beamAt / searchOrigin: no matching overloaded function found`，整屏黑。
- 根因：新函数插在 volumetric() 前面，但 shade() 在更前面就调用了它们；GLSL 没有前向声明就不能后定义。
- 修法：把探照灯三个函数移到 windowLight/shade 之前。
- 下次如何避免：往 shader 插新函数时按"被谁调用"决定位置，工具函数一律放到 map() 之后、shade() 之前的固定区段。

## 6. M6：verify 保证不坏，不保证好看（20:14–20:30）
- 症状：M0–M5 verify 11 次全绿，但 M6 一开始按新直方图标准跑，dark5=0.134（要 <0.03）——画面从来没有真正的黑，整体"灰泥/阴天"，我自己看截图时只判断"像不像城市"没判断"像不像作品"。
- 根因：验证标准只覆盖"坏"（黑屏、品红、掉帧），没有一条覆盖"好"；而"好"的最低限度其实可以量化（黑场、高光、色温统一），我没在 M0 就把它写进 verify，导致 5 个里程碑的审美问题都靠最后 subagent 一次性抓。
- 修法：verify 加 dark5<0.03、bright1>0.9 两条硬指标；艺术方向浓缩成两句话（两种颜色、中间全黑）写进 CLAUDE.md，每次看截图对照。
- 下次如何避免：任何视觉项目在 M0 就把"审美底线"写成 2–3 条可量化指标进 verify（黑场比例、高光比例、主色相方差），并把艺术方向的一句话放在 CLAUDE.md 顶部；每次看截图先问"符合那句话吗"再问"有 bug 吗"。另外：审美指标之间会耦合（暗角同时影响黑场和高光），调参先想清楚每个旋钮动哪一端。

## 7. M6 第 8 步：playwright 自带 ffmpeg 是精简版（20:28）
- 症状：`ffmpeg -f null -` 报 "Requested output format 'null' is not known"，录屏帧数解析成 0，verify 误报。
- 根因：ms-playwright 的 ffmpeg-mac 只带录屏需要的编码器和 muxer，不是通用 ffmpeg；系统也没装 ffprobe。
- 修法：让 Chrome 回放 webm，读 `getVideoPlaybackQuality().totalVideoFrames`。
- 下次如何避免：依赖外部二进制前先跑一次 `--version`/最小命令确认能力；能用浏览器自己做的校验优先用浏览器。

## 8. M7 第 6 步：verify 没过却 commit 了（00:45）
- 症状：fps 48 < 50，verify 退出码 1，但 `node verify.mjs | grep ... && git commit` 里 grep 的退出码盖掉了 verify 的，commit 照常发生。
- 根因：把 verify 和 commit 串在一条管道里，且用 grep 过滤输出；管道退出码是最后一个命令的。
- 修法：改成 `set -o pipefail` 或先 verify 存退出码再决定 commit；本条目发生后立即补做性能修复再重新 verify。
- 下次如何避免：CLAUDE.md 规则——verify 和 commit 不写在同一条管道里；commit 前必须看到 `VERIFY PASS` 字样出现在最近一次输出中。

## 9. M7：`fract(sin)` 式哈希让画面每次加载都不一样（00:50）
- 症状：新加的 flicker 指标在相机不动时也报 4%；同一 URL 加载两次，近处楼的窗户点亮模式不同；uniforms 逐项比对完全一致。
- 根因：`hash21` 用 `fract(p*123.34)` + `dot(p,p)` 这类大数浮点乘积取小数，fp32 下低位全是舍入噪声；shader 每次编译（有无 GPU 程序缓存）浮点重排不同，结果就不同。M0–M6 一直如此，只是没有指标能看见。
- 修法：所有哈希改成整数哈希（输入量化到 1/64 → uint 混洗），场景和后期 shader 都换；flicker 从 4% 降到 0.02%。
- 下次如何避免：GLSL 哈希一律用整数版本；任何"应该可复现"的截图在 M0 就做一次"同 URL 加载两次做差"的确定性检查并写进 verify（这次 flicker 指标是无意中抓到的）。

## 10. M8：外部 GPU 占用让 fps 指标失真（01:15）
- 症状：第 2 步 fps 从 57 掉到 30；二分改动无效；checkout 上一 commit 也是 30。
- 根因：甲方浏览器里打开的作品页面在后台以 60 fps 渲染，GPU 被分走一半；机器无过热记录。
- 修法：请甲方关页面；开发期间 FPS_SOFT=1 让 fps 只报告，其余指标照常卡；最后统一重跑。
- 下次如何避免：verify 开头检测"上一 commit 的基线 fps"或直接读 GPU 占用，fps 掉一半先怀疑环境；CLAUDE.md 加"fps 异常先跑基线"。

## 11. M9：GLSL 保留字和缩进匹配（01:55）
- 症状：`half` 作变量名导致整帧黑（verify 抓到，未 commit）；三次 python 补丁因目标行缩进不同被 assert 拦下，没写入。
- 根因：GLSL 保留字表比想象中长（half/short/fixed/input/output 等）；补丁用整行精确匹配，而录屏块在 M6 被移出时改过缩进。
- 修法：改名 hw2；补丁改用正则匹配行首缩进。
- 下次如何避免：变量名避开常见图形 API 保留字；补丁脚本一律用正则或去掉行首空白后匹配；每条 assert 失败就是一次免费的"没有静默改坏"。

## 12. M9：电池供电让整机锁 30 Hz（01:15–）
- 症状：fps 30 且 frameMs 精确 33.3，两条渲染路径一样；`__bench` 的 GPU 计时也不合理。
- 根因：笔记本在电池 13% 放电，macOS 降频 + 刷新率限制；不是 GPU 争抢。
- 修法：请甲方插电；全程 FPS_SOFT，插电后重跑。
- 下次如何避免：verify 开头打印 `pmset -g batt` 状态，电池模式直接标红；CLAUDE.md 加规则。

## 13. M10：命令行进程调用语音识别直接 SIGABRT（11:25）
- 症状：`tools/lyrics` 裸二进制和放进 .app 里直接执行都崩，crash report 说缺 NSSpeechRecognitionUsageDescription，但 Info.plist 明明有。
- 根因：TCC 把权限归到"负责进程"——从终端启动时负责进程是终端/宿主 app，读的是它的 plist。
- 修法：`open -W -a Lyrics.app --args …` 经 LaunchServices 启动，结果写到音频旁边的 .lrc；第一次弹系统授权框由甲方点。
- 下次如何避免：任何要用 macOS 隐私权限（麦克风/语音/相机）的小工具，一开始就做成 .app 并用 open 启动，别走裸二进制。

## 14. M10：世界空间雨第一版尺度错（11:30）
- 症状：近层雨丝在屏幕上成了斜的霓虹管。
- 根因：把雨幕放在 1.4 单位处、雨丝 0.55 长，没有先按"屏幕上应占几个像素"反推世界尺寸。
- 修法：三层推远到 2.4/5.5/12，雨丝 0.22×层缩放，宽度减半，亮度封顶。
- 下次如何避免：屏幕空间效果搬到世界空间时，先写下目标像素尺寸，再换算世界尺寸。

## 15. M10：更多霓虹 vs 亮斑上限（11:40）
- 症状：霓虹密度 0.6 + 广告牌让基准帧（camz=44，正对霓虹带）亮斑 132、dark5 0.038，verify 挂。
- 根因：基准帧碰巧在霓虹带边上；"光的层级"指标和"更赛博朋克"的要求在同一帧上冲突。
- 修法：基准帧移到非霓虹带（camz=100），霓虹帧单独用宽松阈值报告；密度 0.45；亮斑上限 110 并在 PLAN 写明理由。
- 下次如何避免：不同"look"各有一张基准帧和一套阈值，不要用一张帧管所有段落。

## 16. M10：AVAudioFile 不到作用域结束不落盘（16:30）
- 症状：分段识别每段都报 "No speech detected"，而整文件识别正常。
- 根因：写分段 wav 的 AVAudioFile 对象在识别期间还活着，文件头没写完。
- 修法：写入放进独立 do 作用域，对象释放即 flush。
- 下次如何避免：任何"写文件→立刻给另一个 API 读"的流程，先确认句柄已关闭；日志里记每段的段数，这次就是靠 .lrc.log 定位的。

## 17. M10：远景飞行器灯用 1/角度² 没封顶，近相机时成了大光球（16:30）
- 症状：高空和对塔机位里出现两个巨大白球。
- 根因：点光的角度衰减在角度→0 时无界，机位换到高空后灯离相机很近。
- 修法：封顶 2.5 并整体减 4 倍。
- 下次如何避免：任何 1/x 型亮度都必须有上限；新机位加进来时把所有"相机相对"的效果（雨、水珠、灯串）都在每个机位重看一遍。

## 18. M11：歌词被电影黑边挡住，而我的测试窗口刚好看不出来（22:40，甲方纠正）
- 症状：甲方说看不到歌词；我的 playwright 截图里明明有。
- 根因：文字按整块画布的比例定位（0.86 高度处），没有扣掉后期加的 2.39:1 黑边。我的测试窗口是 1720x720，宽高比 2.39，黑边正好是 0 像素；甲方用 16:9 窗口，黑边 92 像素，歌词整行落在黑边里。
- 修法：drawText 里按 `barH = (h - w/2.39)/2` 算出可见区，标题和歌词都定位在可见区内，字号也按可见高度算。
- 下次如何避免：verify 的窗口尺寸不能只有一个，尤其不能刚好等于画面的目标宽高比；任何叠加层（文字、UI）都要按"最终可见区域"定位，不是按画布。规则已加进 CLAUDE.md。

## 19. M11：全屏后歌词不再跟着走（甲方纠正）
- 症状：全屏播放时歌词停在某一句不再变化。
- 根因：三层叠加。(a) 歌词的更新写在渲染循环里；(b) 渲染循环在窗口 `blur` 时暂停，而 macOS 进全屏会把窗口移到新 Space 并触发 blur；(c) 音频是独立元素，照常播放，于是画面和歌词一起冻住而歌声继续。
- 修法：只有 `document.hidden` 才暂停渲染，`blur` 不再暂停（音乐可视化本来就该在失焦时继续跑）；歌词和标题改由自己的 80ms 定时器 + 音频 `timeupdate` 驱动，与渲染彻底解耦；`resize` 和 `fullscreenchange` 时清掉文字缓存并立刻重排（黑边高度随宽高比变化）。
- 下次如何避免：任何跟随媒体时间的显示都不能挂在渲染循环上，要挂在媒体自己的时钟上。规则已加进 CLAUDE.md。

## 20. M11：verify 只跑 ?test=1，漏掉整个真实页面（同一次修复中）
- 症状：我为修 19 打的补丁把非测试模式的页面完全打挂（`player`/`t0` 在声明前被引用，TDZ 报错，歌单一首都不显示），而 verify 依然 PASS。
- 根因：`?test=1` 会跳过 `if (!TEST)` 里的全部初始化，也就是真实用户走的那条路，verify 从来没覆盖过它。
- 修法：verify 增加"真实页面冒烟检查"——不带 test 参数加载（优先本地 http），任何 pageerror/console error 即失败，并断言歌单渲染、歌词条数、歌词随播放位置改变。
- 下次如何避免：测试开关跳过的代码路径必须另有一条测试覆盖；`if (!TEST)` 是一个危险信号，看到就要问"这段谁来测"。

## 21. M11：python http.server 不支持 Range，音频跳转静默失败（同一次修复中）
- 症状：`player.currentTime = 9.8` 之后读回来是 1.2，跳转被吃掉，歌词测试因此误判。
- 根因：`python3 -m http.server` 不实现 HTTP Range，浏览器只能在已缓冲区间内跳转。
- 修法：写了 `tools/serve.mjs`（30 行，支持 206 Partial Content 和 no-store），`npm run serve` 改用它。
- 下次如何避免：涉及媒体的本地开发一律用支持 Range 的服务器；`curl -I -H "Range: bytes=0-99"` 应返回 206。

## 22. M12：立面上的虚线描边和弧线（甲方指出画面"业余"时我自己发现的）
- 症状：静帧放大后，每扇窗户外围有一圈 1 像素亮虚线，立面上还横着一条点状弧线。
- 根因：两个独立缺陷，都在 `fetchScene` 里，和场景着色无关。
  (a) 弧线 = 景深的 `if (coc < 0.6) return`。这是个硬分支，而"离相机等距"在平面上的轨迹是圆锥曲线，投影到屏幕就是一条弧；阈值两侧一边锐利一边模糊，边界就显形。
  (b) 虚线 = FXAA 作用在 HDR 数据上。窗户亮度 4.5、墙面 0.02，FXAA 沿边缘混合时把窗户的亮度拖进墙里，方向向量在相邻像素间翻转，就成了虚线。
- 修法：景深阈值降到 0.35 并用 smoothstep 把模糊淡入，两侧连续；FXAA 结果钳制在 5 个采样的 min/max 之内，并在 TAA 开启时整个跳过（时域超采样已经在做抗锯齿）。
- 下次如何避免：**任何按距离的硬分支都会在屏幕上画出一条弧**——景深、LOD、雾、效果开关，一律用 smoothstep 过渡。抗锯齿要在色调映射之后做，在 HDR 上做会把高光拖成镶边。
- 诊断方法记一笔：先二分"场景 vs 后期"，再在后期内部二分。我第一次二分时以为绕过了后期，其实调试输出仍然经过 `fetchScene`，白白排除了四个无辜的嫌疑（色散、蒸汽、TAA、雾、霓虹点光）。**绕过要绕到底，确认调试路径真的跳过了被测代码。**

## 23. M12 任务二：以为新几何把性能砍了 10 倍，其实是后台进程（我自己的规则救了我）
- 症状：加完楼型词汇后 `__bench` 报每帧 75–193 毫秒，而当天早些时候同样的场景只要 10–18 毫秒。
- 处理：按 CLAUDE.md 的规则先 checkout 上一个 commit 的 index.html 跑基线——基线也是 145 毫秒。于是查系统，发现 `MediaAnalysis`（macOS 媒体分析守护进程）占用 111% CPU。
- 结论：新几何的真实代价是 **+27%**，不是 10 倍。如果没有先跑基线，我会为了一个不存在的问题把刚做好的形状全部砍掉。
- 下次如何避免：规则有效，继续执行。补充一条：绝对性能数字在共享机器上永远要配一个同时测量的基线，只有相对值可信。

## 24. M12 任务二：亮斑数量指标被几何多样性误伤
- 症状：楼型多样化后 `blobs` 从 87 涨到 130，超过 110 的阈值，verify 失败。
- 根因：这个指标数的是「亮度 > 0.8 的连通区域个数」，本意是防止光源泛滥。但同样多的光被更丰富的轮廓切成更多独立区域时，数量会上升而光并没有变多。
- 修法：不是直接抬阈值，而是先量证据——亮部总面积 1.037% → 1.117%，基本不变，证明层级没坏。然后加了一条直接测「亮部面积占比 ≤1.6%」的硬指标（不受几何数量影响），把亮斑数放宽到 150 作为次要护栏。
- 下次如何避免：指标失败时先问「它测的是不是我真正关心的东西」。代理指标（数量）会被无关变化误伤，直接指标（面积）不会。阈值可以改，但必须先拿证据证明意图没被破坏。

## 25. M12 任务三：玻璃材质做对了物理，却做错了表达
- 症状：给玻璃幕墙做了低反照率 + 菲涅尔反射 + 环境近似后，玻璃楼变成了没有细节的黑块，比之前统一的中灰更难看。我连续调了反照率、菲涅尔基值、环境亮度三轮，收效甚微。
- 根因：物理上没错——夜里没亮灯的玻璃塔本来就是一团黑，之前好看只是因为所有材质都是同一个中灰。真正让玻璃幕墙一眼可辨的不是反射强度，而是**开窗方式**：幕墙是整层连续的横向带状灯光，砖石是一个个凿出来的窗洞。
- 修法：按材质类别改窗格尺寸和点亮相关性——玻璃 1.15×0.40 的横向带且整层同亮同灭，砖石维持原有窗洞。另外补了整个渲染器一直缺的镜面高光（月光、探照灯、霓虹的反光），这样粗糙度才有意义。
- 教训：调了三轮参数没进展时，要问的不是「参数对不对」，而是「我表达这个材质的**渠道**选对了吗」。反射是正确的物理但错误的渠道；开窗方式才是观众读材质的渠道。
- 中途还做了一件对的事：不确定我一直在裁剪的那栋楼是不是玻璃时，用假色输出材质类别确认了前提，而不是继续基于假设调参。

## 26. M12 任务三：带状窗让亮部面积超标，而降低数量无效
- 症状：改成带状窗后亮部面积从 1.11% 跳到 1.83%，超过 1.6% 的上限。把点亮的楼层数减少 40%，只降到 1.72%。
- 根因：超标来自少数很亮的带，不是数量。带的面积是窗洞的六倍，同样的亮度等级铺开后面积就爆了。
- 修法：降低带的**强度**（上限 2.4、整体 ×0.38）而不是数量——带的面积大，本来就不需要窗洞那样的亮度才能读出来。
- 下次如何避免：面积类指标超标时，先分清是「数量多」还是「单个太亮/太大」，两者的修法相反。

## 27. M14：车全部被射线区间裁掉了（看不见但没报错）
- 症状：加了车身几何后画面里一辆车都没有，verify 全绿。
- 根因：车的位置由「该车道最近的车位」算出，可能离它所属格子中心十几个单位；而 DDA 只在该格对应的射线区间 [tCur, tLim] 内测试它，于是几乎全被裁掉。
- 修法：只有中心真正落在格内的车才归这个格（其余留给它真正所在的格），并给射线区间留出车身长度的余量。顺带把车距从 26–38 缩到 11–20，否则「只归属所在格」会让密度掉到每 64 单位一辆。
- 下次如何避免：程序化摆放物件时，「谁负责画它」和「它在哪」必须是同一个格子。位置由周期函数算出时先检查它是否落在负责它的格内。

## 28. M14：先假色再调参，省掉了好几轮瞎猜
- 我在「车到底有没有生成」上猜了两轮（以为是太暗、以为是密度）。改用假色输出材质类别后，一张图就看清了：车在、贩卖机在、招牌在，真正的问题是最近那辆车紧贴镜头成了一块黑楔子。
- 这条和第 25 条是同一个教训的两面：**看不见的东西先确认它存不存在，再讨论它为什么不好看。**

## 29. M14：离线不只是音频的事
- 症状：打包版内嵌了两首歌，断网测试仍然白屏。
- 根因：importmap 还指向 three.js 的 CDN。音频离线了，引擎没有。
- 修法：把 three.module.js 也做成 data: URI 写进 importmap（import map 接受 data: 地址）。
- 下次如何避免：声称「离线可用」前，用 route abort 把所有 http 请求掐掉再跑一遍，而不是只断音频源。

## 30. M14：一个黑球，五次误诊（甲方要求零缺陷时发现）
- 症状：河面机位塔顶悬着一个深色球体。
- 我依次误判为：飞行器灯、体积雾空洞、探照灯近场硬切、灯具位于桅杆内部被自身遮挡、景深虚化。每一次都改了代码，每一次都毫无效果。
- 真因：体积散射用「视线与光轴最近接近点 ± 窗口」采样。正对光源看时这套几何退化（`den = 1 - b²` → 0），而我为保护退化情况写的每一个分支——除爆的 tc、零宽窗口的回退、光照侧裁剪——都会在屏幕上画出一个属于自己的硬边圆。关掉景深后看到边缘是锐利的正圆，才确认是二值分支而非模糊。
- 修法：放弃开窗，改成对整段可见距离做 12 步无分支均匀步进。没有边界条件，就没有圆。
- 下次如何避免：**锐利的圆 = 某个二值判断的等值面**。先关掉所有模糊（景深、bloom、TAA）确认边缘是硬的还是软的，硬边直接去找 if/step/clamp，别再调参数。另外：一个"保护性分支"往往就是下一个缺陷的来源，能用无分支的均匀方法就别开窗。

## 31. M14：自适应分辨率救不了帧率，因为后期一直在满分辨率跑
- 症状：实时运行时自适应分辨率已经降到下限 0.4（688×360），fps 仍只有 34–49。
- 根因：renderScale 只作用于场景 pass。后期合成（FXAA 5 次 + 景深 12 次 + bloom 4×9 次 + 变形拖影 9 次，每像素约 60 次纹理取样）用的是窗口分辨率，与 renderScale 无关。所以降分辨率省下的远少于预期。
- 修法：把整条后期链移到渲染分辨率，只把颗粒、暗角、黑边、片门微动和文字留在满分辨率的最终合成（这些每像素只有几次运算，而且在原生分辨率上才正确）。同分辨率下 fps 从 34–49 升到 42–60。
- 下次如何避免：加自适应分辨率时，逐个 pass 确认它到底跟不跟 renderScale 走；一个不跟的重 pass 会让整套机制失效。

## 32. M16：分区之后，两个代理指标该退役了
- 症状：加了城市分区（contiguous zoning）之后，基准帧的「亮斑连通区数量」和「亮度重心偏离」双双擦线失败。这已经是同类问题第三次出现。
- 判断：先问这两个指标测的是不是我真正关心的东西。
  - 亮斑数量的意图是「光的层级」，而亮部面积占比直接测同一件事，且不受几何数量影响——**代理被直接指标完全覆盖，退役是合并不是放宽**。
  - 亮度重心的意图是「构图别死正中」，但它测的是光的分布而不是相机的位置；密集繁华街两侧对称是这个街区本来的性格，而相机构图（偏轴、偏航、地平线在下三分）写死在代码里根本没变。降到 2.5 作为弱护栏，并写明理由。
- 黑场、高光、暖色占比、亮部面积、flicker 五项一律没动。
- 下次如何避免：代理指标要定期回头看它是否已被更直接的指标取代；被同一类变化混淆两次以上就该重新设计而不是继续调阈值。

## 33. M16：Python 补丁脚本在末尾才写文件，一条断言失败整批回滚
- 症状：一次加入高架和河桥的补丁里有一条断言失败，我以为只有那一条没生效，于是单独补了下游的 hook——结果着色器引用了从未插入的函数，直接黑屏。
- 根因：脚本是先做全部替换、最后 `open().write()`。任何一条 assert 抛出，前面所有改动一起丢，但我按「只有失败那条没生效」去补救。
- 修法：重新完整应用整批。
- 下次如何避免：多处改动的补丁，要么每处独立写盘，要么失败后重跑整批——不要假设部分生效。

## 34. M18：一张基准帧撑不起一座有分区的城市
- 症状：分区做完之后，同一套阈值在工业带和繁华街上给出完全不同的读数（亮部面积 0.97%–11.22%，最暗 5% 均值 0.000–0.038）。为了让唯一那张基准帧通过，我这一轮已经退役了一个指标、放宽了两个——安全网在变薄，而不是城市在变好。
- 根因：单帧基准隐含一个前提——城市在统计上是均匀的。分区做完之后这个前提不再成立，于是每次调阈值其实是在给某一个街区特调。
- 修法：改成 11 帧的基准组，一个浏览器跑完，覆盖每个分区和每处基础设施。**通用规则**（黑场、高光、暖色、品红、非黑、flicker）每帧都必须成立；**只有亮部面积按帧给上限**，因为长焦对着地标本来就大半是光。上限全部按实测值加三成余量，写在表里。
- 顺带纠正了两个错误认知：(a) 我一直以为 camz=100 是繁华街，实际是高层区——单帧基准的危险正在于此；(b) 对塔帧之前测到的 11.22% 亮部面积全是光圈扩散造成的，它的真实值是 1.79%，原来的上限 14% 完全是虚的。
- 构图检查也换了：亮度重心测的是光落在哪，密集街区两侧对称会误判（已经误判三次）。改为直接断言相机参数——偏离中线 0.90、偏航 6.0°、俯仰 6.7°。**测那个东西本身，别测它的影子。**
- 下次如何避免：当被测对象本身变成非均匀的，基准也必须跟着变成一组；发现自己在反复为通过阈值而调阈值时，问题多半在基准的代表性上。

## 35. M19：那颗"月亮"从来没有出现在画面里
- 症状：甲方说天空缺东西。我去看天空，发现 shader 里的主光叫"月光"但天上没有月亮。补了月亮之后截图里出现一团刺眼的白药丸和一弯诡异的月牙——我一度以为那就是我画的月亮被云切开了。
- 根因两层：(a) 那团药丸根本不是月亮，是飞机灯（`min(..., 2.5)` 的硬上限把一盏远灯削成了一个饱和圆面）被 anamorphic streak 横向拉开；我的月亮其实一直在画外。(b) 主光方向 `(-0.52, 0.58, 0.63)` 是"看着像左前上方"随手写的，但 2.39:1 画幅只有 ±1.0 的纵向余量，偏轴方向会被透视放大顶出上边缘——实测五个机位全部落在画面上方 1000–1700 px 处。
- 中间还走了一段弯路：我用"把 uBloomStr 设成 0"当对照实验来排除后期，结果图一模一样，于是判定"是场景本身画的"。实际上 anamorphic streak 那一项 `col += streak/wsum * ...` 根本不乘 uBloomStr，这个开关没关掉它。和 M10 那次"绕过后期链的调试其实还走 fetchScene"是同一个错误。
- 修法：把方向当成可测量的量——在页面里用相机基向量把候选方向投影成屏幕坐标，对 5 个机位 × 20 个候选方向做网格搜索，选出全部进画的 19° 仰角 / 36° 偏角。月亮峰值从 1.55 降到 0.92（刚过 bloom 阈值），临边昏暗、月海、盈凸相位才不会被 clip 掉——**要保留表面细节的亮物体，峰值必须留在 clip 以下**。飞机灯换成高斯点 + 淡晕。
- 下次如何避免：要出现在画面里的世界方向，先投影成屏幕坐标验证，别靠看截图猜；把某个 pass "关掉"做对照实验之前，先确认这个开关真的覆盖了它的每一项。

## 36. M19：天空底色同时是雾的颜色
- 症状：加完天空后 entertainment 帧的 dark5 从 0.038 涨到 0.040，正好卡在"必须有真正的黑"的阈值上。查最暗 5% 像素的分布，89% 在画面下半部——不在天上。
- 根因：我把地平线辉光加进了 `skyBase`，而 `skyBase` 有六处是当雾的颜色用的（`mix(outc, skyBase(rd)*vec3(0.75,0.95,1.2), ...)`）。改天空的同时把全城远景的雾一起抬暖了一层。
- 修法：辉光移进 `skyColor`，`skyBase` 只留渐变。dark5 回到 0.038，天空的辉光一点没少。
- 下次如何避免：改一个 shader 函数之前先 grep 它的调用点。被两个语义共用的函数（天空底色 / 雾色）改一处会静默改另一处，而 verify 只会在某一帧的某个指标上冒出 0.002 的漂移。

## 37. M19：我把「没看过它动起来」写进了待办，甲方说你先看
- 症状：我给甲方列「距离成品还差什么」，第一条自己写的就是"我从来没有看过它动起来"，然后把它当成一个待办放着。甲方直接说：你先看一遍。看完之后，列表上排第一的问题根本不是我写的那些——**真实歌曲下，街道机位有三分之一的时间整个画面是一片白**。
- 根因：verify 的 11 张基准帧全部跑 test 模式，`beat` 默认 99 ＝没有节拍。唯一一张带节拍的 beatring 用的是长焦对塔的机位，我给它的亮部面积上限是 15%。同一个节拍放到街道机位上是亮部 10.9% / 过曝 2.5%（无节拍时是 2.08% / 0.01%），而且真实歌里节拍环每 0.5 秒重发、满强度保持 0.4 秒、同时追两个环——城市几乎没有回到暗的时候。「光少而准」这条艺术方向在实际播放时大部分时间不成立，而我的验证里没有任何一项能看见这件事。
- 中途又踩了一次「开关没接上」：我用 `&beat=0.1` 做对照，三张图完全一样——0.1 秒的环半径只有 4 米，根本没照到画面里的楼（要 0.9）。同一次里 `uBloomThr` 的覆写也被渲染循环每帧写回去了。**两张数字完全相同 ＝ 开关没接上，不是「这个因素无关」。**
- 修法：写了 `tools/watch.mjs`——真实页面放歌、按固定间隔抓帧，同时记录 rig / fps / section / 歌词，再拼成联系表看。这一次看出来的还有：HUD 面板默认一直在画面里、闪电亮得像白天、街道机位会贴到楼面上、自适应分辨率掉到 0.55。
- 下次如何避免：静帧验证只能证明「没坏」，证明不了「好看」，更证明不了「放着歌的时候好看」。**任何由外部输入驱动的效果（音频、时间、交互），必须有一条跑真实输入的观察通道**；每个里程碑结束前看一遍动的，和跑 verify 一样是固定动作。

## 38. M20：看完录屏之后修的四件事，以及两次自己纠正自己
- 症状一：我在 480px 宽的联系表缩略图上判断"街道机位贴到了楼面上"，写进了给甲方的清单。开原图一看，那两帧都是正常的街景，近处是一块招牌，构图没问题。真正的问题藏在同一张原图的面板里：`scale 0.40 · 688x288`。
- 根因一：用缩略图做构图判断。缩略图能告诉我"哪一帧值得看"，不能告诉我"这一帧出了什么事"。
- 症状二：把道具的硬剔除换成"离相机轴的横向距离"之后，station 帧亮部面积从 5.43% 跳到 10.59%，office 从 2.19% 跳到 3.25%——一台发光的自动售货机站在镜头两米外。
- 根因二：我换判据的时候只想着"侧面经过的东西会自己出画，所以不用剔"。这对*消失得明不明显*是对的，对*占画面多大*是错的。一个 2.2 米外的箱子占掉三分之一画高，跟它在不在光轴上没有关系。**距离才是主轴，方向只决定「什么时候可以直接丢掉」。**
- 修法：`lensFade` 改成以距离为主的 smoothstep，方向只用来判断"已经和镜头齐平或在身后 → 直接丢，没人看见它走"。三处硬剔除（售货机 7m、行人 6m、车 14m）全部变成缩放。改完 12 帧数值和改动前一模一样——说明静帧里本来就没有道具落在过渡带上，这个改动对截图零影响，只在运动中起作用。
- 另外两件：自适应分辨率原来是"低于 55 降、高于 59 升"，在一台永远到不了 59 的机器上就是单向棘轮，一分钟内走到 0.4 地板并永远留在那里（整部片子渲染在 688×288）。**迟滞区间的两端都必须是够得着的。** 地板提到 0.6，但如果 0.6 连 26 fps 都撑不住就让地板让步——卡顿的推轨比软的推轨更糟。面板改成放歌 2.5 秒后淡出、鼠标一动回来。
- 下次如何避免：缩略图只用来定位，判断回原图；换掉一个判据之前先讲清楚原判据在防什么（这里防的是"占画面太大"，不是"突然消失"）。

## 39. M21：楼体没有纹理，而我先去加光
- 症状：甲方说"所有建筑都是黑底，没有合适的纹理"。开原图量了一下墙面：窗户 0.40–0.65，窗户之间的墙 0.02–0.05——确实就是纯黑上面浮着几个亮方块。
- 根因（三层，我是一层层挖下去的）：
  1. **浮雕只改了反照率**。原来的楼板线和竖挺是 `alb *= mix(0.5, 1.0, slab*mull)`。一面本来就黑的墙乘 0.5 还是黑。要让接缝读出来必须**改法线**，让缝的两颊分别吃到不同的光。
  2. **改完法线还是看不见，因为那面墙上根本没有光**。主光在它背后，天空环境光几乎无方向性（0.55+0.45n.y 在竖墙上等于常数）。浮雕需要有方向的光才会显形。
  3. **窗户的溢光只照亮了它自己那一格**。`smoothstep(1.1, 0.25, Ld)` 的衰减在相邻格（Ld≈1.0）只剩 4%，而墙面绝大部分像素都在相邻格里。改成"窄芯 + 宽尾"之后，每一扇亮着的窗才真的在墙上画出一圈光——而这正好是局部的：没开灯的楼依然是黑的。
- 走过的弯路：我先加了一个全局的街道反弹光（0.22），entertainment 帧的 dark5 从 0.038 冲到 0.048，oldtown 也挂了。**均匀地照亮整座城市**既把楼重新压平，又把画面里的黑全吃掉了。第二次我又把 skyAmb 对朝下面的响应提了 3 倍，结果高架桥底面变成灰色——**朝下的面看不到天空，它看到的是街**。
- 关键取舍：最后能看见的纹理，几乎全部来自**不花光的手段**——法线浮雕、接缝压暗（0.5→0.36）、板块反照率抖动（±24%）、窗户溢光。反弹光从 0.22 一路退到 0.030。entertainment 的 dark5 回到 0.038，和改动前一模一样，安全网一点没松。
- 差点做错的事：entertainment 卡在 0.040 时我一度准备给它单独放宽阈值，理由是"实测基线帧本来就只有 0.6% 的像素低于 0.03，这一帧从来就没有真正的黑"。这个证据是真的，但**我有一个几乎不花代价的替代方案（少加光、多加对比）**，那就没有资格去松安全网。先穷尽替代方案，再谈改阈值。
- 下次如何避免：要"加细节"时先问光够不够——没有光的地方加任何贴图都等于没加；能用重新分配（对比、接缝、法线）解决的，不要用抬亮度解决，因为抬亮度一定会撞上黑场。

## 40. M22：那块"发光的板"是一栋盖在马路上的房子
- 症状：station 帧左下角有一大块纯奶油色的平板，是全画面最亮的东西，里面什么都没有。我先当成"整层带状玻璃没有竖挺"去修——给幕墙加了竖挺、给每个开间加了亮度差异，数字动了一点，那块板几乎没变。
- 找法：把 `shade()` 开头临时改成按材质输出纯色（红=建筑、绿=地面、紫=看板…），一次渲染就读出来了——**红色，材质 1，是建筑**。不是看板，不是招牌。
  - 第一次我输出的是 `float(mat)/32.0` 灰度，读回来是 4.6 / 7.4 / 5.6 这种非整数——**调试值也要过色调映射和颗粒**。改成用色相编码才读得回来。
- 根因：车站大厅的盒子用的是固定半宽 `CELL*0.5 - 0.9`，而城市里其他所有地块都用 `lotSpanX()` 按街道宽度内缩。街道半宽在某些列是 1.15，于是大厅的墙正好落在相机自己的行进线上（x=-0.9），一扇亮着的窗被放大到占了四分之一画面。
- 修法：大厅改用 `lotSpanX()`，SDF 和追踪器包围盒一起改。station 帧亮部 5.43%→1.89%、暖色 8.4%→4.1%、真正的黑 0.020→0.029，而且 **A/B 追踪器一致性从 1.81% 降到 1.61%**——这栋越界的房子本来就是两条路径分歧的一部分。
- 下次如何避免：看到"一块没有细节的纯色"先查它是什么，别先假设它是什么再去修那个假设。材质染色只要一次渲染就能回答，比三轮参数便宜得多。

## 41. M23：给行人加伞，然后把它删了
- 症状：路上在下雨，行人却没有伞——看起来是个明显该补的细节。加完之后渲出来是一根**悬在头顶的黑色横杠**。
- 根因：街道机位的相机高度是 1.5–4.0 m，伞面在 1.74 m。一把 7 m 外的伞，相机和伞面的高度差是 0.24 m ——**视线和伞面的夹角只有两度**，永远是正侧面。一个圆盘正侧面看就是一根线；而伞的下表面朝下，几乎接不到任何光，于是是一根**黑**线。
- 修法：删掉。不是参数不对，是这个细节在这个机位高度上永远不成立。保留了同一轮里加的行人边缘光（用最近的路灯/霓虹做菲涅尔边缘），那个是有效的。
- 顺带一个有用的结果：我先怀疑伞很贵，用 `window.__bench(40)` 跑了三组交错对比——**有伞 116/118/132 ms，无伞 117/124/131 ms，在噪声里**。fps 的下降全部来自机器降频（当时电池 9%）。**别用 fps 的印象判断开销，用 bench 的中位数。**
- 下次如何避免：加一个新的小几何体之前，先算它在实际机位下张多大角、朝哪面、被什么照亮。"下雨了所以该有伞"是对的观察，"所以加一个圆盘"是错的实现。

## 42. M24：每一块袖看板上的字都是反的，而我差点没查出来
- 症状：截图里一块绿色的横看板，我读出来是「H4S 替両」——反的。修了一个"双面招牌两面共用同一个 u"的问题，渲出来一模一样（逐像素零差异）。
- 走的弯路：我连续四次靠**肉眼读模糊、倾斜、被景深糊过的日文字**来判断方向，每次都得出不同的结论。先以为是横看板反了，再以为是材质 12，用材质染色查出来其实是材质 13。
- 转折：把判断从"读字"换成**在图集每个格子的左端画红条、右端画蓝条**。一次渲染就能看出图集的左端落在屏幕的哪一边，完全不需要认字。再补一个指向右的白三角，方向更是毫无歧义。
  - 结果一：那块横看板**本来就是正的**。我从头到尾都在误读被糊掉的字形。
  - 结果二：拿同一套色标去量**竖的袖看板**，逐行配对红蓝：**81 行全部是蓝在左、红在右——每一块正对相机的袖看板上的字都是镜像的。** 修完是 80 行全对、0 行错。
- 根因：袖看板是一块薄板（e.z=0.05），两面朝 ±z。两面共用 `u = (p.x-c.x)/e.x`。看 +z 面时屏幕右方是 +x，正确；看 -z 面时屏幕右方是 -x，于是整块字是镜像。而相机的推轨永远沿 +z 走，**看到的永远是那个镜像的面**——也就是说城市里数量最多的一类招牌，玩家看到的每一块都是反的。
- 下次如何避免：**方向类的问题不要用「读内容」来判断，往内容里塞一个不对称的标记（色标、三角）再看。** 肉眼读低分辨率 + 景深模糊的文字，得到的结论比抛硬币还差——它会让你自信地去修一个不存在的 bug。

## 43. M25：黑场阈值第三次挡住正确的改动，这次我去量了它
- 症状：给霓虹加真实的面光/遮挡之后，entertainment 帧的 dark5 一再顶在 0.040 上。这是它第三次挡住一个我认为正确的改动（招牌浮雕一次、街道反弹一次、这次两次）。
- 我先做的事（和前两次一样）：穷尽不花钱的替代方案。发射端余弦让 dark5 从 0.045 回到 0.037——**光更强了，底反而更低了**，因为它把侧向的糊光砍掉了。这是这一轮最有价值的一步，也证明了"先找重新分配、再谈抬阈值"是对的。
- 然后我量了全部 13 帧的黑含量（低于 0.03 的像素占比）：普通街景 1.4%–4.4%，两张霓虹区帧 **0.64% 和 0.97%**。**它们不是同一个分布。** 一个满墙发光招牌的街区本来就没有黑，真实的歌舞伎町也没有。在那两帧上，"最暗 5% 的均值"量的不是黑场，是一张亮照片里最暗的部分。
- 修法：dark5 从通用规则改成**逐帧上限**，和 brightArea 当初改成逐帧是同一个理由。每帧按自己实测值留三成余量：**13 帧里有 9 帧的上限比原来的 0.040 更严**（downtown 0.033、river 0.033、aerial 0.006、tower 0.004），两张霓虹帧 0.046。另加一条通用兜底 dark5max=0.055，防止任何一帧变成灰糊。
- 下次如何避免：同一个指标第三次挡住不同的正确改动时，先别再让步——去量它所声称测量的那个东西在各帧上的分布。如果分布明显不是一个总体，那它就不该是一条通用规则。**但顺序不能反：先穷尽重新分配，再动阈值。**

## 44. M25：我把 verify 和 commit 写在了同一条管道里
- 症状：`FPS_SOFT=1 node verify.mjs 2>&1 | tail -8 && git add -A && git commit` —— tail 的退出码是 0，于是一个 **entertainment 帧失败的状态被提交了**（dark5 0.047 ≥ 0.046）。
- 根因：CLAUDE.md 里有一条规则，一字不差就是讲这件事的："verify 和 commit 不写在同一条管道里（grep 会吞退出码）"。我为了少滚一屏输出，顺手把它们串起来了。
- 修法：验证一律 `node verify.mjs > /tmp/v.log 2>&1; echo "exit=$?"` 再看日志，退出码和输出分开。这次的失败本身也修了（袖看板辉光的横向范围是招牌自身宽度的 3 倍，近处一面墙就占了很大屏幕面积；收到 2 倍并降幅度）。
- 下次如何避免：规则写下来不等于会被遵守——**任何时候在验证命令后面接 `&&`，先问一句退出码还在不在**。已经在 CLAUDE.md 里的规则，重犯一次说明它需要一个更难违反的形式，而不是更醒目的措辞。

## 45. M25：全局光照这一轮，四次尝试里三次被同一件事否掉
- 甲方要的是"霓虹真的照亮对面的墙"。做成的四件事和被否掉的四件事，分界线非常清楚：
  - **成了**：霓虹管当线光源积分（形状和衰减都对了）、**发射端余弦**（管子贴在墙上，光射向街道而不是沿着自己那面墙抹开）、两点遮挡、以及最有效的一条——**招牌在自己那面墙上留下的辉光**（改动量 0.0106，是本轮其他任何一项的四倍，而且完全免费，因为它搭在已经在跑的招牌代码里）。
  - **否了**：袖看板当面光源（+18% 帧时间换 0.0027 改动量）、湿地面的镜像光源（0.0019 改动量却吃掉 0.002 黑场预算）、管子离墙 5cm→20cm（0.0003，发射端余弦在掠射角把它杀掉了）、以及**三次"给全城每个街区都加发光体"的尝试**——店招（+19% 且 downtown 0.025→0.038、oldtown 0.026→0.044）、租户指示牌（entertainment 0.044→0.051）。
- **规律**：局部的发光（霓虹区的管子、每块招牌自己那面墙）能成立；**任何"每个街区都有"的发光都会以黑场为代价，而且代价远大于收益**。夜景的黑不是背景，是构图本身。
- 另一条规律：**真正有效的往往不是"更好的光传输"，而是"这个东西本来就该自己发光"**。墙上那圈辉光在物理上不是面光源能产生的（共面互相看不见），但它在每一张霓虹灯照片里都有——因为它属于招牌，不属于光传输。四次里最有效的一次是想通了这件事。
- 下次如何避免：加 GI 之前先问"这是局部的还是全城的"。全城的，先算它会吃掉多少黑场再动手。

## 46. M26：模糊和帧率是同一个问题
- 甲方两条反馈："什么都是模糊的，没有焦点"和"一直 20–30 帧，有没有不动质量的优化"。量完发现这是同一件事：帧率上不去 → 自适应分辨率停在 0.45 → 整幅画面被放大 4.4 倍。
- **量化诊断**（不是猜）：
  - 用 shader 自己的公式算每个机位的景深：**街道机位只有 8.0–10.5 米是清晰的**，而 9 米处通常是空马路。城市里的楼（3–100m）、招牌（4–40m）、街道纵深全部在景深之外。这就是"没有焦点"。
  - 用高频能量（原图减高斯）分区量：远景（30–90m）因为景深损失 **30%** 的细节；renderScale 0.45→1.0 是 **+56%**。**分辨率是主因，景深是次因但同样真实。**
- **性能**：逐项 profile 出 `calcNormal` 占整帧 **34%**——网格追踪器解的是解析求交，它本来就知道命中了哪个面，然后着色器把这个信息扔掉，再用四次 map() 的有限差分去重新发现它。让 hitBox/hitCylY/hitSphere 在命中时记下法线，**54 个调用点一个都不用改**（三个图元自己维护最小值）。86.2ms → 53.3ms，**降 38%**，画面平均差 0.0006，而且不一致的 0.69% 像素（全在轮廓边缘）上解析法线才是对的。
- 连锁效果：自适应分辨率从 0.45 升到 **0.6**（像素数 1.8 倍），实测帧率同时从 20–30 升到 **27–48**。**更快和更清晰不是取舍，是同一个修复。**
- 没做成的一件：`raymarch` 里那唯一一次 map() 调用（只为了取材质）实测占 **45%**。同样的办法本该也能拿掉，但 `lotSDF` 的材质是用"累加器 + 末尾覆盖"的语义决定的（`if (br < b){ mat = 7; }`），和追踪器按图元分组的结构不是一一对应——五张帧上 0.4%–13% 的像素材质对不上，会肉眼可见地改变着色。回退了。
- 下次如何避免：**两条看起来无关的抱怨，先算一遍它们是不是同一个根因**。另外，"把 X 从 A 挪到 B"这类重构，先写一个能逐像素比对 A 和 B 的调试输出再动手——法线那次我是靠图像差验证的，材质那次正是这个比对救了我，没让一个 13% 错误的改动进仓库。

## 47. M27：天空的直边，和一个把整排楼变成铁皮的 bug
- **天空**：云是对一个 value noise 场做阈值，而 value noise 常用的三次淡入 `f*f*(3-2f)` 只有 C1——二阶导在每个格子边界上跳变。一旦阈值化，这些跳变就变成沿着噪声格子走的**长直折边**，也就是甲方截图里那些成块的楔形。改成五次淡入 + 每个八度旋转（两者都免费，实测 +1.1%）。试过再加畸变和第四个八度：云形更好看，+3.4%，而瑕疵在这之前就已经没了——撤了。
  - **关键细节**：五次淡入不能直接改 `vnoise`，因为 `districtOf` 也用它——全局改会**把城市重新分区**，neonstreet 帧的黑场从 0.037 跳到 0.053。单独开了一个 `cnoise` 只给云用。修天气不该重划城市。
- **顺带挖出的真 bug**：`lotSDF` 开头设 `mat = 1`，然后 `viaductSDF` 和 `civicSDF` 各自在路过时设 `mat = 7`，而建筑的距离算完之后**没人把它设回来**。于是**高架所在的那一行（每 13 行一次）上的所有楼都是材质 7**：裸金属反照率，窗格、窗套、立面浮雕全部跳过（那条路径以 `mat == 1 || mat == 5` 为门）。river 帧里 20% 是材质 7，其中 11 个百分点是这个。
  - 它是怎么被发现的：我在做"追踪器直接报材质"的重构，发现两边有 13% 的像素对不上。**我没有假设新代码错了，而是去问哪一边错了。**结果是旧的那边。
- **性能**：`raymarch` 里那唯一一次 map() 调用（只为取 gMat）用"禁用法"量出来是 45%，实际是 **13%**。禁用法把调用去掉后 `gMat` 变成常量，编译器顺手把 `shade()` 里一大半也折叠掉了——**那次测量同时测掉了材质和材质下游的一切**。去掉一个值，和去掉产生这个值的工作，不是同一个实验。
- 下次如何避免：噪声一旦要被阈值化，就必须至少 C2；一个被多处共用的基础函数（vnoise）改之前先 grep 用它的地方。
