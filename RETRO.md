# RETRO

## 1. M1 第一版曝光像白天（19:05，推翻自己方案）
- 症状：第一次 verify 通过但截图是白天雾城，楼面接近纯白。
- 根因：albedo 0.24、key 0.85、雾 0.032、天空地平线 0.30 都是按"看得清"的日景量级填的，没先定夜景的绝对亮度基准。
- 修法：所有线性量整体降一档：albedo 0.13、key ×0.6、雾 0.011、地平线 0.11，两轮才收敛。
- 下次如何避免：写第一版光照前先定 3 个锚点值（天空最亮处、楼面受光面、楼面背光面的线性亮度目标），其余参数往锚点推，而不是先写再调。

## 2. M3 窗户第一版像乐高（19:28，推翻自己方案）
- 症状：窗户占格子 50%、2.4 倍亮度、密度 0.4，整面墙是黄白方块，远处闪。
- 根因：窗户发光强度和尺寸没有参照"唯一暖色点缀"的定位，按"看得见"给的；远处没有 LOD。
- 修法：窗口缩到格子 40%，亮度 0.9，密度上限 0.5，远处按距离退化成平均亮度。
- 下次如何避免：点缀元素先给"占画面亮度多少比例"的目标（这里是 <10% 像素、峰值不超过天空最亮处 3 倍），再定参数。

## 3. gMat 被 calcNormal 覆盖（19:29，M3 第 7 步发现）
- 症状：写反射时意识到主射线的材质 id 是在 calcNormal 之后读的，交界处会串材质。
- 根因：map() 用全局变量传材质，任何额外的 map() 调用（法线、AO、体积光）都会改写它。
- 修法：raymarch 返回后立刻读 gMat，再算法线。
- 下次如何避免：规则——全局副作用变量必须在产生它的调用之后一行内消费；或者让 raymarch 直接返回材质。

## 4. 时间记录不可靠（19:29）
- 症状：PLAN.md 里 M2/M3 的时间戳是我估的，与 `date` 输出不符，改了两次。
- 根因：没有在每个里程碑开始/结束时真的跑 `date`。
- 修法：里程碑开始和 commit 前各跑一次 `date` 写进 PLAN.md。
- 下次如何避免：CLAUDE.md 加规则（已加）。

## 5. M6 第 5 步 shader 编译失败：函数定义顺序（20:41）
- 症状：verify 报 `beamAt / searchOrigin: no matching overloaded function found`，整屏黑。
- 根因：新函数插在 volumetric() 前面，但 shade() 在更前面就调用了它们；GLSL 没有前向声明就不能后定义。
- 修法：把探照灯三个函数移到 windowLight/shade 之前。
- 下次如何避免：往 shader 插新函数时按"被谁调用"决定位置，工具函数一律放到 map() 之后、shade() 之前的固定区段。

## 6. M6：verify 保证不坏，不保证好看（20:14–20:30）
- 症状：M0–M5 verify 11 次全绿，但 M6 一开始按新直方图标准跑，dark5=0.134（要 <0.03）——画面从来没有真正的黑，整体"灰泥/阴天"，我自己看截图时只判断"像不像城市"没判断"像不像作品"。
- 根因：验证标准只覆盖"坏"（黑屏、品红、掉帧），没有一条覆盖"好"；而"好"的最低限度其实可以量化（黑场、高光、色温统一），我没在 M0 就把它写进 verify，导致 5 个里程碑的审美问题都靠最后 subagent 一次性抓。
- 修法：verify 加 dark5<0.03、bright1>0.9 两条硬指标；艺术方向浓缩成两句话（两种颜色、中间全黑）写进 CLAUDE.md，每次看截图对照。
- 下次如何避免：任何视觉项目在 M0 就把"审美底线"写成 2–3 条可量化指标进 verify（黑场比例、高光比例、主色相方差），并把艺术方向的一句话放在 CLAUDE.md 顶部；每次看截图先问"符合那句话吗"再问"有 bug 吗"。另外：审美指标之间会耦合（暗角同时影响黑场和高光），调参先想清楚每个旋钮动哪一端。

## 7. M6 第 8 步：playwright 自带 ffmpeg 是精简版（20:28）
- 症状：`ffmpeg -f null -` 报 "Requested output format 'null' is not known"，录屏帧数解析成 0，verify 误报。
- 根因：ms-playwright 的 ffmpeg-mac 只带录屏需要的编码器和 muxer，不是通用 ffmpeg；系统也没装 ffprobe。
- 修法：让 Chrome 回放 webm，读 `getVideoPlaybackQuality().totalVideoFrames`。
- 下次如何避免：依赖外部二进制前先跑一次 `--version`/最小命令确认能力；能用浏览器自己做的校验优先用浏览器。

## 8. M7 第 6 步：verify 没过却 commit 了（00:45）
- 症状：fps 48 < 50，verify 退出码 1，但 `node verify.mjs | grep ... && git commit` 里 grep 的退出码盖掉了 verify 的，commit 照常发生。
- 根因：把 verify 和 commit 串在一条管道里，且用 grep 过滤输出；管道退出码是最后一个命令的。
- 修法：改成 `set -o pipefail` 或先 verify 存退出码再决定 commit；本条目发生后立即补做性能修复再重新 verify。
- 下次如何避免：CLAUDE.md 规则——verify 和 commit 不写在同一条管道里；commit 前必须看到 `VERIFY PASS` 字样出现在最近一次输出中。

## 9. M7：`fract(sin)` 式哈希让画面每次加载都不一样（00:50）
- 症状：新加的 flicker 指标在相机不动时也报 4%；同一 URL 加载两次，近处楼的窗户点亮模式不同；uniforms 逐项比对完全一致。
- 根因：`hash21` 用 `fract(p*123.34)` + `dot(p,p)` 这类大数浮点乘积取小数，fp32 下低位全是舍入噪声；shader 每次编译（有无 GPU 程序缓存）浮点重排不同，结果就不同。M0–M6 一直如此，只是没有指标能看见。
- 修法：所有哈希改成整数哈希（输入量化到 1/64 → uint 混洗），场景和后期 shader 都换；flicker 从 4% 降到 0.02%。
- 下次如何避免：GLSL 哈希一律用整数版本；任何"应该可复现"的截图在 M0 就做一次"同 URL 加载两次做差"的确定性检查并写进 verify（这次 flicker 指标是无意中抓到的）。

## 10. M8：外部 GPU 占用让 fps 指标失真（01:15）
- 症状：第 2 步 fps 从 57 掉到 30；二分改动无效；checkout 上一 commit 也是 30。
- 根因：甲方浏览器里打开的作品页面在后台以 60 fps 渲染，GPU 被分走一半；机器无过热记录。
- 修法：请甲方关页面；开发期间 FPS_SOFT=1 让 fps 只报告，其余指标照常卡；最后统一重跑。
- 下次如何避免：verify 开头检测"上一 commit 的基线 fps"或直接读 GPU 占用，fps 掉一半先怀疑环境；CLAUDE.md 加"fps 异常先跑基线"。

## 11. M9：GLSL 保留字和缩进匹配（01:55）
- 症状：`half` 作变量名导致整帧黑（verify 抓到，未 commit）；三次 python 补丁因目标行缩进不同被 assert 拦下，没写入。
- 根因：GLSL 保留字表比想象中长（half/short/fixed/input/output 等）；补丁用整行精确匹配，而录屏块在 M6 被移出时改过缩进。
- 修法：改名 hw2；补丁改用正则匹配行首缩进。
- 下次如何避免：变量名避开常见图形 API 保留字；补丁脚本一律用正则或去掉行首空白后匹配；每条 assert 失败就是一次免费的"没有静默改坏"。

## 12. M9：电池供电让整机锁 30 Hz（01:15–）
- 症状：fps 30 且 frameMs 精确 33.3，两条渲染路径一样；`__bench` 的 GPU 计时也不合理。
- 根因：笔记本在电池 13% 放电，macOS 降频 + 刷新率限制；不是 GPU 争抢。
- 修法：请甲方插电；全程 FPS_SOFT，插电后重跑。
- 下次如何避免：verify 开头打印 `pmset -g batt` 状态，电池模式直接标红；CLAUDE.md 加规则。

## 13. M10：命令行进程调用语音识别直接 SIGABRT（11:25）
- 症状：`tools/lyrics` 裸二进制和放进 .app 里直接执行都崩，crash report 说缺 NSSpeechRecognitionUsageDescription，但 Info.plist 明明有。
- 根因：TCC 把权限归到"负责进程"——从终端启动时负责进程是终端/宿主 app，读的是它的 plist。
- 修法：`open -W -a Lyrics.app --args …` 经 LaunchServices 启动，结果写到音频旁边的 .lrc；第一次弹系统授权框由甲方点。
- 下次如何避免：任何要用 macOS 隐私权限（麦克风/语音/相机）的小工具，一开始就做成 .app 并用 open 启动，别走裸二进制。

## 14. M10：世界空间雨第一版尺度错（11:30）
- 症状：近层雨丝在屏幕上成了斜的霓虹管。
- 根因：把雨幕放在 1.4 单位处、雨丝 0.55 长，没有先按"屏幕上应占几个像素"反推世界尺寸。
- 修法：三层推远到 2.4/5.5/12，雨丝 0.22×层缩放，宽度减半，亮度封顶。
- 下次如何避免：屏幕空间效果搬到世界空间时，先写下目标像素尺寸，再换算世界尺寸。

## 15. M10：更多霓虹 vs 亮斑上限（11:40）
- 症状：霓虹密度 0.6 + 广告牌让基准帧（camz=44，正对霓虹带）亮斑 132、dark5 0.038，verify 挂。
- 根因：基准帧碰巧在霓虹带边上；"光的层级"指标和"更赛博朋克"的要求在同一帧上冲突。
- 修法：基准帧移到非霓虹带（camz=100），霓虹帧单独用宽松阈值报告；密度 0.45；亮斑上限 110 并在 PLAN 写明理由。
- 下次如何避免：不同"look"各有一张基准帧和一套阈值，不要用一张帧管所有段落。

## 16. M10：AVAudioFile 不到作用域结束不落盘（16:30）
- 症状：分段识别每段都报 "No speech detected"，而整文件识别正常。
- 根因：写分段 wav 的 AVAudioFile 对象在识别期间还活着，文件头没写完。
- 修法：写入放进独立 do 作用域，对象释放即 flush。
- 下次如何避免：任何"写文件→立刻给另一个 API 读"的流程，先确认句柄已关闭；日志里记每段的段数，这次就是靠 .lrc.log 定位的。

## 17. M10：远景飞行器灯用 1/角度² 没封顶，近相机时成了大光球（16:30）
- 症状：高空和对塔机位里出现两个巨大白球。
- 根因：点光的角度衰减在角度→0 时无界，机位换到高空后灯离相机很近。
- 修法：封顶 2.5 并整体减 4 倍。
- 下次如何避免：任何 1/x 型亮度都必须有上限；新机位加进来时把所有"相机相对"的效果（雨、水珠、灯串）都在每个机位重看一遍。
