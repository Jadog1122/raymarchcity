# POSTMORTEM — raymarchcity（2026-09-08 19:01–19:53）

## 1. 时间线：计划 vs 实际
| 里程碑 | 预算 | 实际 | 备注 |
|---|---|---|---|
| 准备 | — | 2 min | playwright + chromium 安装即完成 |
| M0 骨架 + verify | 15 | 3 | 一次 verify 通过 |
| M1 城市 SDF + 光照 + 雾 | 25 | 4 | 4 轮 verify，3 轮在调曝光 |
| M2 音频 | 15 | 6 | 含自建合成 wav 跑真实路径 |
| M3 映射 + 相机 + 体积光 | 25（硬止损） | 3 | 7 步 8 次 verify，全部完成 |
| M4 性能 + 面板 | 15 | 3 | |
| M5 审阅 + 录屏 | 15 | 4 | subagent 跑 90 s |
| 合计 | 120 | ≈25 分钟工作 + 等 CP | 没有任何一步超时；等甲方回复占了大头 |

超时原因：无。多次 verify 每次 ~8 s，是节奏而不是瓶颈。

## 2. RETRO 条目归类
| # | 条目 | 类别 | 项目特有 / 通用 |
|---|---|---|---|
| 1 | 第一版曝光像白天 | 领域（图形） | 通用：任何"先写再调"的视觉项目都会撞，先定亮度锚点 |
| 2 | 窗户像乐高 | 领域（美术） | 通用：点缀元素先给画面占比目标 |
| 3 | gMat 被 calcNormal 覆盖 | 领域（GLSL 模式） | 项目特有于 raymarch，但"全局副作用变量被后续调用覆盖"是通用坑 |
| 4 | 时间戳靠估 | 人（我） | 通用：任何 vibe 项目都会，除非规则化 `date` |
环境类：0 条（DPR、file://、麦克风授权都在任务书里预先写死了，没踩）。工具类：0 条（playwright 有头模式直接给了真实 GPU 和 60 fps）。

## 3. 起作用 / 无效的做法
起作用（有证据）：
- **verify 回路先于一切**：11 次 verify 全绿，每次 8 s；shader 改动从未出现过黑屏或品红，任何回归都会在下一次 verify 被抓。
- **任务书把环境事实写死**（DPR=2、renderScale 基于 CSS、有头模式）：`__stats` 第一次就报 rtW=1290（=1720×0.75），没有走弯路。
- **playwright 代替甲方做真实路径验证**：M2 用合成 wav、M4 用滑杆 dispatch、M5 用 download 事件，都在甲方看之前抓到了"能跑"的证据，CP2/CP4 甲方一次通过。
- **subagent 只给截图 + shader 不给对话**：9 条意见里 3 条直接命中最大问题（冷暖雾抵消、整体中灰、beat 像 glitch），修完截图对比明显更像夜。
- **硬止损 + 每步 verify 的 M3**：7 步都做完，没有一步因为叠加改动而找不到回归来源。
无效 / 反效果：
- PLAN.md 里的时间戳我估过两次，被 `date` 打脸，浪费了两次修正 commit。
- 审阅意见里有 4 条与任务书冲突（地平线冷色、bass 幅度），subagent 没读任务书就必然会提，这部分是可预期的噪音。
- 每次 verify 后我用 Read 看截图和下一步 edit 并行，有覆盖 latest.png 的竞态风险（没真的撞上，但不该赌）。

## 4. 重来一次该改的 5 处
1. verify.mjs 一开始就把截图按 `M<n>-<hhmm>` 复制并打印路径，我读带时间戳的那份，杜绝并行竞态。
2. M1 动手前先写三个线性亮度锚点值（天空 / 受光面 / 背光面），第一轮就是夜景。
3. raymarch 直接返回 `vec2(t, mat)`，不用全局 gMat。
4. 审阅 subagent 的 prompt 里附上"甲方硬性要求"清单（暖地平线、bass 0.6 等），让它只在约束内挑刺，减少无效意见。
5. 里程碑开始/结束跑 `date` 自动写 PLAN.md（或者让 verify.mjs 顺手 append）。

## 5. CLAUDE.md 最终版
见仓库 CLAUDE.md（全文同时贴在 CP5 消息里）。
